apiVersion: v1
kind: Pod
metadata:
  name: {{ include "windmill.fullname" . }}-test-connection
  labels:
    {{- include "windmill.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: health-check
      image: curlimages/curl:8.11.0
      command:
        - curl
        - --fail
        - --silent
        - --show-error
        - "http://{{ include "windmill.fullname" . }}:{{ .Values.service.httpPort }}/api/version"
